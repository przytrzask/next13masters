fragment ProductListItem on Product {
	id
	name
	slug
	description
	price
	rating
	ratingCount
	images(first: 10) {
		url
	}
	categories(first: 10) {
		id
		name
	}

	colors {
		id
		color
	}

	sizes {
		id
		size
	}
}
